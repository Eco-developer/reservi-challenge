/** @type {import('next').NextConfig} */
const nextConfig = {  
  images: {  
    domains: ['http2.mlstatic.com'], // Agrega aquí el dominio que estás utilizando  
  },  
}  
export default nextConfig;
